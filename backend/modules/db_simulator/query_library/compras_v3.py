"""
query_library/compras_v3.py — 150 consultas adicionales de Compras (v3).
DEVIA: backend/modules/db_simulator/DEVIA.md
Compatibilidad SQLite. Sin comentarios subjetivos.
"""
from backend.modules.db_simulator.query_library.builder import q

QUERIES_COMPRAS_V3 = [
    q("cx3_001","Proveedores con más artículos en catálogo","¿Qué proveedor suministra más artículos?","Conteo de artículos por PROVEEDDEFECTO en ARTICULO.","SELECT COALESCE(P.NOMBRECOMERCIAL,P.RAZONSOCIAL) AS PROVEEDOR, COUNT(*) AS N_ARTICULOS, ROUND(AVG(A.PRECIOCOSTE),2) AS COSTE_MEDIO FROM ARTICULO A LEFT JOIN PROVEED P ON P.CODIGO=A.PROVEEDDEFECTO WHERE A.PROVEEDDEFECTO>0 GROUP BY A.PROVEEDDEFECTO,P.NOMBRECOMERCIAL,P.RAZONSOCIAL ORDER BY N_ARTICULOS DESC LIMIT 20","Compras","Dirección","Proveedor","Medio","Catálogo",""),
    q("cx3_002","Artículos sin PRECIOCOSTE de compra definido","¿Qué artículos tienen PRECIOCOSTE=0?","Artículos con PRECIOCOSTE=0 o NULL que impiden calcular margen.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,ROUND(PRECIOVENTA,2) AS PVP FROM ARTICULO WHERE PRECIOCOSTE=0 OR PRECIOCOSTE IS NULL ORDER BY NOMBRE LIMIT 30","Compras","Almacén","Artículo","Alto","Datos maestros",""),
    q("cx3_003","Ratio PRECIOCOSTE/PRECIOVENTA de venta por artículo","¿Qué artículos tienen mayor PRECIOCOSTE relativo?","PRECIOCOSTE/PRECIOVENTA por artículo. Ratio alto = margen bajo.","SELECT CODIGO,NOMBRE,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(PRECIOVENTA,2) AS PVP,ROUND(PRECIOCOSTE*100.0/NULLIF(PRECIOVENTA,0),1) AS RATIO_COSTE_PCT FROM ARTICULO WHERE PRECIOCOSTE>0 AND PRECIOVENTA>0 ORDER BY RATIO_COSTE_PCT DESC LIMIT 20","Compras","Dirección","Artículo","Alto","Margen",""),
    q("cx3_004","Artículos con PRECIOCOSTE superior al PRECIOVENTA de venta","¿Hay artículos donde PRECIOCOSTE>PRECIOVENTA?","Artículos con margen negativo en tarifa.","SELECT CODIGO,NOMBRE,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(PRECIOVENTA,2) AS PVP,ROUND(PRECIOCOSTE-PRECIOVENTA,2) AS DIFERENCIA FROM ARTICULO WHERE PRECIOCOSTE>PRECIOVENTA AND PRECIOVENTA>0 ORDER BY DIFERENCIA DESC LIMIT 20","Compras","Dirección","Artículo","Crítico","Margen",""),
    q("cx3_005","Valor total de compras por proveedor (via PRECIOCOSTE)","¿Cuánto representa cada proveedor en el PRECIOCOSTE de ventas?","Suma de PRECIOCOSTE*CANTIDAD vendidas por proveedor en facturas TIPO=13.","SELECT COALESCE(P.NOMBRECOMERCIAL,P.RAZONSOCIAL) AS PROVEEDOR, ROUND(SUM(A.PRECIOCOSTE*L.CANTIDAD),2) AS COSTE_VENTAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO LEFT JOIN PROVEED P ON P.CODIGO=A.PROVEEDDEFECTO WHERE D.TIPO=13 AND A.PRECIOCOSTE>0 GROUP BY A.PROVEEDDEFECTO,P.NOMBRECOMERCIAL,P.RAZONSOCIAL ORDER BY COSTE_VENTAS DESC LIMIT 20","Compras","Dirección","Proveedor","Alto","COGS",""),
    q("cx3_006","Artículos con mayor rotación de STOCKARTICULO","¿Qué artículos se reponen con más frecuencia?","Movimientos positivos en ESTALMACEN por artículo.","SELECT A.CODIGO,A.NOMBRE,COUNT(*) AS N_ENTRADAS,ROUND(SUM(A.STOCKARTICULO),2) AS TOTAL_ENTRADO FROM ARTICULO E LEFT JOIN ARTICULO A ON A.CODIGO=A.CODIGO WHERE A.STOCKARTICULO>0 GROUP BY A.CODIGO,A.NOMBRE ORDER BY N_ENTRADAS DESC LIMIT 20","Compras","Almacén","Artículo","Medio","Reposición",""),
    q("cx3_007","Artículos con STOCKARTICULO bajo mínimo que necesitan reposición","¿Qué artículos hay que reponer urgentemente?","Artículos con STOCKARTICULO<STOCKARTICULO y STOCKARTICULO>0.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,STOCKARTICULO AS MINIMO,STOCKARTICULO-STOCKARTICULO AS DEFICIT,ROUND(PRECIOCOSTE,2) AS COSTE_UNITARIO FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO<STOCKARTICULO ORDER BY DEFICIT DESC LIMIT 20","Compras","Almacén","Artículo","Crítico","Reposición",""),
    q("cx3_008","PRECIOCOSTE estimado de reposición de artículos bajo mínimo","¿Cuánto costaría reponer los artículos bajo mínimo?","Suma de (STOCKARTICULO-STOCKARTICULO)*PRECIOCOSTE para artículos bajo mínimo.","SELECT COUNT(*) AS N_ARTICULOS,ROUND(SUM((STOCKARTICULO-STOCKARTICULO)*PRECIOCOSTE),2) AS COSTE_REPOSICION FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO<STOCKARTICULO AND PRECIOCOSTE>0","Compras","Dirección","KPI","Alto","Reposición",""),
    q("cx3_009","Proveedores con mayor número de artículos bajo mínimo","¿Qué proveedor tiene más artículos que reponer?","Artículos bajo mínimo agrupados por proveedor.","SELECT COALESCE(P.NOMBRECOMERCIAL,P.RAZONSOCIAL) AS PROVEEDOR,COUNT(*) AS N_ARTICULOS_BAJO_MINIMO,ROUND(SUM((A.STOCKARTICULO-A.STOCKARTICULO)*A.PRECIOCOSTE),2) AS COSTE_REPOSICION FROM ARTICULO A LEFT JOIN PROVEED P ON P.CODIGO=A.PROVEEDDEFECTO WHERE A.STOCKARTICULO>0 AND A.STOCKARTICULO<A.STOCKARTICULO GROUP BY A.PROVEEDDEFECTO,P.NOMBRECOMERCIAL,P.RAZONSOCIAL ORDER BY N_ARTICULOS_BAJO_MINIMO DESC LIMIT 20","Compras","Almacén","Proveedor","Alto","Reposición",""),
    q("cx3_010","Artículos con STOCKARTICULO cero y ventas recientes","¿Qué artículos se han agotado pero se siguen vendiendo?","Artículos con STOCKARTICULO=0 pero con ventas en los últimos 90 días.","SELECT A.CODIGO,A.NOMBRE,A.STOCKARTICULO AS STOCKARTICULO,ROUND(V.VENTAS_90D,2) AS VENTAS_90D FROM ARTICULO A JOIN (SELECT L.CODARTICULO,SUM(L.PRECIO*L.CANTIDAD) AS VENTAS_90D FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.TIPO=13 AND D.FECHA>=DATE('now','-90 days') GROUP BY L.CODARTICULO) V ON A.CODIGO=A.CODIGO WHERE A.STOCKARTICULO=0 ORDER BY VENTAS_90D DESC LIMIT 20","Compras","Almacén","Artículo","Crítico","Rotura STOCKARTICULO",""),
    q("cx3_011","Evolución de entradas de STOCKARTICULO por mes","¿Cuántas entradas de STOCKARTICULO hay cada mes?","Movimientos positivos en ESTALMACEN por mes.","SELECT SUBSTR(FECHA,1,7) AS MES,COUNT(*) AS N_ENTRADAS,ROUND(SUM(STOCKARTICULO),2) AS TOTAL_UNIDADES FROM ARTICULO WHERE STOCKARTICULO>0 AND FECHA IS NOT NULL GROUP BY SUBSTR(FECHA,1,7) ORDER BY MES DESC LIMIT 24","Compras","Almacén","Operacional","Bajo","STOCKARTICULO",""),
    q("cx3_012","Evolución de salidas de STOCKARTICULO por mes","¿Cuántas salidas de STOCKARTICULO hay cada mes?","Movimientos negativos en ESTALMACEN por mes.","SELECT SUBSTR(FECHA,1,7) AS MES,COUNT(*) AS N_SALIDAS,ROUND(ABS(SUM(STOCKARTICULO)),2) AS TOTAL_UNIDADES FROM ARTICULO WHERE STOCKARTICULO<0 AND FECHA IS NOT NULL GROUP BY SUBSTR(FECHA,1,7) ORDER BY MES DESC LIMIT 24","Compras","Almacén","Operacional","Bajo","STOCKARTICULO",""),
    q("cx3_013","Artículos con mayor número de movimientos de STOCKARTICULO","¿Qué artículos tienen más actividad de almacén?","Conteo total de movimientos en ESTALMACEN por artículo.","SELECT A.CODIGO,A.NOMBRE,COUNT(*) AS N_MOVIMIENTOS FROM ARTICULO E LEFT JOIN ARTICULO A ON A.CODIGO=A.CODIGO GROUP BY A.CODIGO,A.NOMBRE ORDER BY N_MOVIMIENTOS DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","STOCKARTICULO",""),
    q("cx3_014","Artículos sin movimientos de STOCKARTICULO en 6 meses","¿Qué artículos no han tenido movimiento de almacén en 6 meses?","Artículos sin registros en ESTALMACEN en los últimos 180 días.","SELECT A.CODIGO,A.NOMBRE,A.STOCKARTICULO AS STOCKARTICULO FROM ARTICULO A WHERE A.CODIGO NOT IN (SELECT DISTINCT CODIGO FROM ARTICULO WHERE FECHA>=DATE('now','-180 days')) AND A.STOCKARTICULO>0 ORDER BY A.NOMBRE LIMIT 30","Compras","Almacén","Artículo","Medio","STOCKARTICULO inmovilizado",""),
    q("cx3_015","Valor de inventario por proveedor","¿Cuánto vale el STOCKARTICULO de cada proveedor?","Suma de STOCKARTICULO*PRECIOCOSTE agrupado por proveedor.","SELECT COALESCE(P.NOMBRECOMERCIAL,P.RAZONSOCIAL) AS PROVEEDOR,COUNT(*) AS N_ARTICULOS,ROUND(SUM(A.STOCKARTICULO*A.PRECIOCOSTE),2) AS VALOR_STOCKARTICULO FROM ARTICULO A LEFT JOIN PROVEED P ON P.CODIGO=A.PROVEEDDEFECTO WHERE A.STOCKARTICULO>0 AND A.PRECIOCOSTE>0 GROUP BY A.PROVEEDDEFECTO,P.NOMBRECOMERCIAL,P.RAZONSOCIAL ORDER BY VALOR_STOCKARTICULO DESC LIMIT 20","Compras","Dirección","Proveedor","Alto","Inventario",""),
    q("cx3_016","Artículos con mayor diferencia entre STOCKARTICULO máximo y actual","¿Qué artículos están más lejos de su STOCKARTICULO máximo?","Diferencia STOCKARTICULO-STOCKARTICULO para artículos con STOCKARTICULO>0.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,STOCKARTICULO AS MAXIMO,STOCKARTICULO-STOCKARTICULO AS DIFERENCIA FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO<STOCKARTICULO ORDER BY DIFERENCIA DESC LIMIT 20","Compras","Almacén","Artículo","Medio","STOCKARTICULO",""),
    q("cx3_017","Proveedores sin compras registradas","¿Qué proveedores no tienen documentos de compra?","Proveedores sin ningún documento en DOCCAB con CODCLIENTE.","SELECT P.CODIGO,COALESCE(P.NOMBRECOMERCIAL,P.RAZONSOCIAL) AS PROVEEDOR FROM PROVEED P WHERE P.CODIGO NOT IN (SELECT DISTINCT CODCLIENTE FROM DOCCAB WHERE CODCLIENTE IS NOT NULL AND CODCLIENTE>0) ORDER BY PROVEEDOR LIMIT 20","Compras","Compras","Proveedor","Bajo","Datos maestros",""),
    q("cx3_018","Artículos con equivalencias (sustitutos disponibles)","¿Qué artículos tienen sustitutos en EQUIVAL?","Artículos con registros en EQUIVAL que permiten sustitución.","SELECT A.CODIGO,A.NOMBRE AS ARTICULO_PRINCIPAL,COUNT(*) AS N_EQUIVALENCIAS FROM ARTICULO LEFT JOIN ARTICULO A ON A.CODIGO=A.CODIGO GROUP BY A.CODIGO,A.NOMBRE ORDER BY N_EQUIVALENCIAS DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Equivalencias",""),
    q("cx3_019","PRECIOCOSTE medio de artículos por familia","¿Cuál es el PRECIOCOSTE medio de los artículos por familia?","Promedio de PRECIOCOSTE por CODFAMILIA en ARTICULO.","SELECT A.CODFAMILIA,F.NOMBRE AS FAMILIA,COUNT(*) AS N_ARTICULOS,ROUND(AVG(A.PRECIOCOSTE),2) AS COSTE_MEDIO,ROUND(AVG(A.PRECIOVENTA),2) AS PVP_MEDIO FROM ARTICULO A LEFT JOIN FAMILIA F ON F.CODIGO=A.CODFAMILIA WHERE A.PRECIOCOSTE>0 GROUP BY A.CODFAMILIA,F.NOMBRE ORDER BY COSTE_MEDIO DESC LIMIT 20","Compras","Dirección","Artículo","Medio","Costes",""),
    q("cx3_020","Artículos con mayor margen sobre PRECIOCOSTE","¿Qué artículos tienen mayor margen sobre su PRECIOCOSTE?","(PRECIOVENTA-PRECIOCOSTE)/PRECIOCOSTE por artículo.","SELECT CODIGO,NOMBRE,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(PRECIOVENTA,2) AS PVP,ROUND((PRECIOVENTA-PRECIOCOSTE)*100.0/NULLIF(PRECIOCOSTE,0),1) AS MARGEN_SOBRE_COSTE_PCT FROM ARTICULO WHERE PRECIOCOSTE>0 AND PRECIOVENTA>PRECIOCOSTE ORDER BY MARGEN_SOBRE_COSTE_PCT DESC LIMIT 20","Compras","Dirección","Artículo","Medio","Margen",""),
    q("cx3_021","Artículos con menor margen sobre PRECIOCOSTE","¿Qué artículos tienen menor margen sobre su PRECIOCOSTE?","(PRECIOVENTA-PRECIOCOSTE)/PRECIOCOSTE por artículo, ordenado ascendente.","SELECT CODIGO,NOMBRE,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(PRECIOVENTA,2) AS PVP,ROUND((PRECIOVENTA-PRECIOCOSTE)*100.0/NULLIF(PRECIOCOSTE,0),1) AS MARGEN_SOBRE_COSTE_PCT FROM ARTICULO WHERE PRECIOCOSTE>0 AND PRECIOVENTA>0 ORDER BY MARGEN_SOBRE_COSTE_PCT ASC LIMIT 20","Compras","Dirección","Artículo","Alto","Margen",""),
    q("cx3_022","Artículos más vendidos con STOCKARTICULO bajo","¿Qué artículos muy vendidos tienen poco STOCKARTICULO?","Artículos con altas ventas en 90 días pero STOCKARTICULO<10 unidades.","SELECT A.CODIGO,A.NOMBRE,A.STOCKARTICULO AS STOCKARTICULO,ROUND(V.VENTAS_90D,2) AS VENTAS_90D FROM ARTICULO A JOIN (SELECT L.CODARTICULO,SUM(L.CANTIDAD) AS VENTAS_90D FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.TIPO=13 AND D.FECHA>=DATE('now','-90 days') GROUP BY L.CODARTICULO) V ON A.CODIGO=A.CODIGO WHERE A.STOCKARTICULO<10 AND A.STOCKARTICULO>=0 ORDER BY VENTAS_90D DESC LIMIT 20","Compras","Almacén","Artículo","Crítico","Rotura STOCKARTICULO",""),
    q("cx3_023","Resumen de STOCKARTICULO por familia","¿Cuánto STOCKARTICULO hay por familia de producto?","Suma de STOCKARTICULO y valor por familia.","SELECT A.CODFAMILIA,F.NOMBRE AS FAMILIA,COUNT(*) AS N_ARTICULOS,ROUND(SUM(A.STOCKARTICULO),2) AS TOTAL_UNIDADES,ROUND(SUM(A.STOCKARTICULO*A.PRECIOCOSTE),2) AS VALOR_PRECIOCOSTE FROM ARTICULO A LEFT JOIN FAMILIA F ON F.CODIGO=A.CODFAMILIA WHERE A.STOCKARTICULO>0 GROUP BY A.CODFAMILIA,F.NOMBRE ORDER BY VALOR_PRECIOCOSTE DESC LIMIT 20","Compras","Almacén","Artículo","Medio","Inventario",""),
    q("cx3_024","Artículos con STOCKARTICULO superior al máximo","¿Qué artículos tienen exceso de STOCKARTICULO?","Artículos con STOCKARTICULO>STOCKARTICULO y STOCKARTICULO>0.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,STOCKARTICULO AS MAXIMO,STOCKARTICULO-STOCKARTICULO AS EXCESO,ROUND((STOCKARTICULO-STOCKARTICULO)*PRECIOCOSTE,2) AS VALOR_EXCESO FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO>STOCKARTICULO ORDER BY EXCESO DESC LIMIT 20","Compras","Almacén","Artículo","Medio","STOCKARTICULO",""),
    q("cx3_025","PRECIOCOSTE total de artículos vendidos por familia","¿Cuánto cuesta vender cada familia de producto?","Suma de PRECIOCOSTE*CANTIDAD por familia en facturas TIPO=13.","SELECT A.CODFAMILIA,F.NOMBRE AS FAMILIA,ROUND(SUM(A.PRECIOCOSTE*L.CANTIDAD),2) AS COGS,ROUND(SUM(L.PRECIO*L.CANTIDAD),2) AS INGRESOS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO LEFT JOIN FAMILIA F ON F.CODIGO=A.CODFAMILIA WHERE D.TIPO=13 AND A.PRECIOCOSTE>0 GROUP BY A.CODFAMILIA,F.NOMBRE ORDER BY COGS DESC LIMIT 20","Compras","Dirección","Artículo","Alto","COGS",""),
    q("cx3_026","Artículos con PRECIOVENTA de PRECIOCOSTE actualizado recientemente","¿Qué artículos tienen PRECIOVENTA de PRECIOCOSTE mayor que el 80% del PVP?","Artículos con PRECIOCOSTE>PRECIOVENTA*0.8 (margen<20%).","SELECT CODIGO,NOMBRE,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(PRECIOVENTA,2) AS PVP,ROUND((PRECIOVENTA-PRECIOCOSTE)*100.0/NULLIF(PRECIOVENTA,0),1) AS MARGEN_PCT FROM ARTICULO WHERE PRECIOCOSTE>PRECIOVENTA*0.8 AND PRECIOVENTA>0 ORDER BY MARGEN_PCT ASC LIMIT 20","Compras","Dirección","Artículo","Alto","Margen",""),
    q("cx3_027","Número de artículos por proveedor con STOCKARTICULO","¿Cuántos artículos con STOCKARTICULO tiene cada proveedor?","Artículos con STOCKARTICULO>0 agrupados por proveedor.","SELECT COALESCE(P.NOMBRECOMERCIAL,P.RAZONSOCIAL) AS PROVEEDOR,COUNT(*) AS N_ARTICULOS_CON_STOCKARTICULO,ROUND(SUM(A.STOCKARTICULO),2) AS TOTAL_UNIDADES FROM ARTICULO A LEFT JOIN PROVEED P ON P.CODIGO=A.PROVEEDDEFECTO WHERE A.STOCKARTICULO>0 AND A.PROVEEDDEFECTO>0 GROUP BY A.PROVEEDDEFECTO,P.NOMBRECOMERCIAL,P.RAZONSOCIAL ORDER BY N_ARTICULOS_CON_STOCKARTICULO DESC LIMIT 20","Compras","Almacén","Proveedor","Bajo","Inventario",""),
    q("cx3_028","Artículos con STOCKARTICULO negativo por proveedor","¿Qué proveedores tienen artículos con STOCKARTICULO negativo?","Artículos con STOCKARTICULO<0 agrupados por proveedor.","SELECT COALESCE(P.NOMBRECOMERCIAL,P.RAZONSOCIAL) AS PROVEEDOR,COUNT(*) AS N_ARTICULOS_STOCK_NEG FROM ARTICULO A LEFT JOIN PROVEED P ON P.CODIGO=A.PROVEEDDEFECTO WHERE A.STOCKARTICULO<0 GROUP BY A.PROVEEDDEFECTO,P.NOMBRECOMERCIAL,P.RAZONSOCIAL ORDER BY N_ARTICULOS_STOCK_NEG DESC LIMIT 20","Compras","Almacén","Proveedor","Alto","STOCKARTICULO",""),
    q("cx3_029","Artículos con mayor número de ventas distintas (popularidad)","¿Qué artículos aparecen en más facturas distintas?","Número de facturas TIPO=13 distintas por artículo.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(DISTINCT L.CODDOCUMENTO) AS N_FACTURAS,SUM(L.CANTIDAD) AS TOTAL_UNIDADES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=13 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_FACTURAS DESC LIMIT 20","Compras","Comercial","Artículo","Medio","Popularidad",""),
    q("cx3_030","Artículos con ventas pero sin STOCKARTICULO","¿Qué artículos se venden pero no tienen STOCKARTICULO registrado?","Artículos con ventas TIPO=13 en 90 días pero STOCKARTICULO=0.","SELECT A.CODIGO,A.NOMBRE,A.STOCKARTICULO AS STOCKARTICULO,ROUND(V.VENTAS,2) AS VENTAS_90D FROM ARTICULO A JOIN (SELECT L.CODARTICULO,SUM(L.PRECIO*L.CANTIDAD) AS VENTAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.TIPO=13 AND D.FECHA>=DATE('now','-90 days') GROUP BY L.CODARTICULO) V ON A.CODIGO=A.CODIGO WHERE A.STOCKARTICULO<=0 ORDER BY VENTAS_90D DESC LIMIT 20","Compras","Almacén","Artículo","Alto","Rotura STOCKARTICULO",""),
    q("cx3_031","Resumen de proveedores activos","¿Cuántos proveedores hay y cuántos tienen artículos?","Conteo de proveedores con y sin artículos asignados.","SELECT COUNT(*) AS TOTAL_PROVEEDORES,(SELECT COUNT(DISTINCT PROVEEDDEFECTO) FROM ARTICULO WHERE PROVEEDDEFECTO>0) AS CON_ARTICULOS,(SELECT COUNT(*) FROM PROVEED)-(SELECT COUNT(DISTINCT PROVEEDDEFECTO) FROM ARTICULO WHERE PROVEEDDEFECTO>0) AS SIN_ARTICULOS FROM PROVEED LIMIT 1","Compras","Dirección","KPI","Bajo","Proveedores",""),
    q("cx3_032","Artículos con PRECIOVENTA de PRECIOCOSTE igual al PRECIOVENTA de venta","¿Hay artículos con margen cero (PRECIOCOSTE=PVP)?","Artículos donde PRECIOCOSTE=PRECIOVENTA.","SELECT CODIGO,NOMBRE,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(PRECIOVENTA,2) AS PVP FROM ARTICULO WHERE PRECIOCOSTE=PRECIOVENTA AND PRECIOCOSTE>0 ORDER BY NOMBRE LIMIT 20","Compras","Dirección","Artículo","Alto","Margen",""),
    q("cx3_033","Artículos con mayor PRECIOCOSTE unitario","¿Qué artículos tienen mayor PRECIOVENTA de PRECIOCOSTE?","Top 20 artículos por PRECIOCOSTE.","SELECT CODIGO,NOMBRE,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(PRECIOVENTA,2) AS PVP,STOCKARTICULO AS STOCKARTICULO FROM ARTICULO WHERE PRECIOCOSTE>0 ORDER BY PRECIOCOSTE DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Costes",""),
    q("cx3_034","Artículos con menor PRECIOCOSTE unitario","¿Qué artículos tienen menor PRECIOVENTA de PRECIOCOSTE?","Top 20 artículos por PRECIOCOSTE ascendente (excluyendo cero).","SELECT CODIGO,NOMBRE,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(PRECIOVENTA,2) AS PVP FROM ARTICULO WHERE PRECIOCOSTE>0 ORDER BY PRECIOCOSTE ASC LIMIT 20","Compras","Almacén","Artículo","Bajo","Costes",""),
    q("cx3_035","Artículos con STOCKARTICULO y sin PRECIOVENTA de venta","¿Hay artículos con STOCKARTICULO pero sin PRECIOVENTA de venta?","Artículos con STOCKARTICULO>0 y PRECIOVENTA=0 o NULL.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,ROUND(PRECIOCOSTE,2) AS COSTE FROM ARTICULO WHERE STOCKARTICULO>0 AND (PRECIOVENTA=0 OR PRECIOVENTA IS NULL) ORDER BY NOMBRE LIMIT 20","Compras","Comercial","Artículo","Alto","Datos maestros",""),
    q("cx3_036","Artículos con STOCKARTICULO y sin PRECIOVENTA de PRECIOCOSTE","¿Hay artículos con STOCKARTICULO pero sin PRECIOVENTA de PRECIOCOSTE?","Artículos con STOCKARTICULO>0 y PRECIOCOSTE=0 o NULL.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,ROUND(PRECIOVENTA,2) AS PVP FROM ARTICULO WHERE STOCKARTICULO>0 AND (PRECIOCOSTE=0 OR PRECIOCOSTE IS NULL) ORDER BY NOMBRE LIMIT 20","Compras","Compras","Artículo","Alto","Datos maestros",""),
    q("cx3_037","Evolución del valor del inventario por mes (estimado)","¿Cómo evoluciona el valor del inventario?","Valor de ESTALMACEN acumulado por mes.","SELECT SUBSTR(FECHA,1,7) AS MES,COUNT(*) AS N_MOVIMIENTOS,ROUND(SUM(STOCKARTICULO),2) AS VARIACION_NETA FROM ARTICULO WHERE FECHA IS NOT NULL GROUP BY SUBSTR(FECHA,1,7) ORDER BY MES DESC LIMIT 24","Compras","Dirección","KPI","Medio","Inventario",""),
    q("cx3_038","Artículos con más de 5 movimientos de STOCKARTICULO en el mes","¿Qué artículos tienen más actividad de almacén este mes?","Artículos con más de 5 movimientos en ESTALMACEN en el mes actual.","SELECT A.CODIGO,A.NOMBRE,COUNT(*) AS N_MOVIMIENTOS FROM ARTICULO E LEFT JOIN ARTICULO A ON A.CODIGO=A.CODIGO WHERE SUBSTR(E.FECHA,1,7)=SUBSTR(DATE('now'),1,7) GROUP BY A.CODIGO,A.NOMBRE HAVING COUNT(*)>5 ORDER BY N_MOVIMIENTOS DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","STOCKARTICULO",""),
    q("cx3_039","Artículos con STOCKARTICULO entre mínimo y máximo (rango óptimo)","¿Qué artículos están en el rango óptimo de STOCKARTICULO?","Artículos con STOCKARTICULO<=STOCKARTICULO<=STOCKARTICULO.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,STOCKARTICULO AS MINIMO,STOCKARTICULO AS MAXIMO FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO>0 AND STOCKARTICULO>=STOCKARTICULO AND STOCKARTICULO<=STOCKARTICULO ORDER BY NOMBRE LIMIT 30","Compras","Almacén","Artículo","Bajo","STOCKARTICULO",""),
    q("cx3_040","PRECIOCOSTE de ventas del mes actual","¿Cuánto ha costado lo vendido este mes?","Suma de PRECIOCOSTE*CANTIDAD en facturas TIPO=13 del mes actual.","SELECT ROUND(SUM(A.PRECIOCOSTE*L.CANTIDAD),2) AS COGS_MES,ROUND(SUM(L.PRECIO*L.CANTIDAD),2) AS INGRESOS_MES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=13 AND A.PRECIOCOSTE>0 AND SUBSTR(D.FECHA,1,7)=SUBSTR(DATE('now'),1,7)","Compras","Dirección","KPI","Alto","COGS",""),
    q("cx3_041","Artículos con mayor número de unidades en STOCKARTICULO","¿Qué artículos tienen más unidades en almacén?","Top 20 artículos por STOCKARTICULO.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(STOCKARTICULO*PRECIOCOSTE,2) AS VALOR FROM ARTICULO WHERE STOCKARTICULO>0 ORDER BY STOCKARTICULO DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Inventario",""),
    q("cx3_042","Artículos con menor número de unidades en STOCKARTICULO (no cero)","¿Qué artículos tienen STOCKARTICULO casi agotado?","Artículos con STOCKARTICULO entre 1 y 5 unidades.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,STOCKARTICULO AS MINIMO,ROUND(PRECIOCOSTE,2) AS COSTE FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO<=5 ORDER BY STOCKARTICULO ASC LIMIT 20","Compras","Almacén","Artículo","Alto","Rotura STOCKARTICULO",""),
    q("cx3_043","Artículos con ventas en el año pero sin STOCKARTICULO","¿Qué artículos se han vendido este año pero no tienen STOCKARTICULO?","Artículos con ventas TIPO=13 en el año actual y STOCKARTICULO=0.","SELECT A.CODIGO,A.NOMBRE,A.STOCKARTICULO AS STOCKARTICULO,ROUND(V.VENTAS,2) AS VENTAS_ANIO FROM ARTICULO A JOIN (SELECT L.CODARTICULO,SUM(L.PRECIO*L.CANTIDAD) AS VENTAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.TIPO=13 AND SUBSTR(D.FECHA,1,4)=SUBSTR(DATE('now'),1,4) GROUP BY L.CODARTICULO) V ON A.CODIGO=A.CODIGO WHERE A.STOCKARTICULO=0 ORDER BY VENTAS_ANIO DESC LIMIT 20","Compras","Almacén","Artículo","Alto","Rotura STOCKARTICULO",""),
    q("cx3_044","Proveedores con artículos de mayor valor de inventario","¿Qué proveedor tiene mayor valor de inventario?","Suma de STOCKARTICULO*PRECIOCOSTE por proveedor.","SELECT COALESCE(P.NOMBRECOMERCIAL,P.RAZONSOCIAL) AS PROVEEDOR,ROUND(SUM(A.STOCKARTICULO*A.PRECIOCOSTE),2) AS VALOR_INVENTARIO FROM ARTICULO A LEFT JOIN PROVEED P ON P.CODIGO=A.PROVEEDDEFECTO WHERE A.STOCKARTICULO>0 AND A.PRECIOCOSTE>0 GROUP BY A.PROVEEDDEFECTO,P.NOMBRECOMERCIAL,P.RAZONSOCIAL ORDER BY VALOR_INVENTARIO DESC LIMIT 20","Compras","Dirección","Proveedor","Alto","Inventario",""),
    q("cx3_045","Artículos con PRECIOVENTA de PRECIOCOSTE mayor que el año anterior (estimado)","¿Qué artículos se venden a PRECIOVENTA más alto que su PRECIOCOSTE histórico?","Artículos donde PRECIOVENTA de venta real supera el PRECIOCOSTE en más del 100%.","SELECT CODIGO,NOMBRE,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(PRECIOVENTA,2) AS PVP,ROUND((PRECIOVENTA-PRECIOCOSTE)*100.0/NULLIF(PRECIOCOSTE,0),1) AS MARGEN_PCT FROM ARTICULO WHERE PRECIOCOSTE>0 AND PRECIOVENTA>PRECIOCOSTE*2 ORDER BY MARGEN_PCT DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Margen",""),
    q("cx3_046","Artículos con STOCKARTICULO y sin familia asignada","¿Hay artículos con STOCKARTICULO pero sin familia?","Artículos con STOCKARTICULO>0 y CODFAMILIA NULL o 0.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,ROUND(PRECIOCOSTE,2) AS COSTE FROM ARTICULO WHERE STOCKARTICULO>0 AND (CODFAMILIA IS NULL OR CODFAMILIA=0) ORDER BY NOMBRE LIMIT 20","Compras","Almacén","Artículo","Bajo","Datos maestros",""),
    q("cx3_047","Artículos con STOCKARTICULO y sin proveedor asignado","¿Hay artículos con STOCKARTICULO pero sin proveedor?","Artículos con STOCKARTICULO>0 y PROVEEDDEFECTO NULL o 0.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,ROUND(PRECIOCOSTE,2) AS COSTE FROM ARTICULO WHERE STOCKARTICULO>0 AND (PROVEEDDEFECTO IS NULL OR PROVEEDDEFECTO=0) ORDER BY NOMBRE LIMIT 20","Compras","Compras","Artículo","Medio","Datos maestros",""),
    q("cx3_048","Resumen de artículos por estado de STOCKARTICULO y proveedor","¿Cómo está el STOCKARTICULO por proveedor?","Artículos con STOCKARTICULO positivo, cero y negativo por proveedor.","SELECT COALESCE(P.NOMBRECOMERCIAL,P.RAZONSOCIAL) AS PROVEEDOR,SUM(CASE WHEN A.STOCKARTICULO>0 THEN 1 ELSE 0 END) AS CON_STOCKARTICULO,SUM(CASE WHEN A.STOCKARTICULO=0 THEN 1 ELSE 0 END) AS SIN_STOCKARTICULO,SUM(CASE WHEN A.STOCKARTICULO<0 THEN 1 ELSE 0 END) AS STOCK_NEG FROM ARTICULO A LEFT JOIN PROVEED P ON P.CODIGO=A.PROVEEDDEFECTO WHERE A.PROVEEDDEFECTO>0 GROUP BY A.PROVEEDDEFECTO,P.NOMBRECOMERCIAL,P.RAZONSOCIAL ORDER BY CON_STOCKARTICULO DESC LIMIT 20","Compras","Almacén","Proveedor","Medio","Inventario",""),
    q("cx3_049","Artículos con mayor PRECIOCOSTE de STOCKARTICULO inmovilizado por proveedor","¿Qué proveedor tiene más STOCKARTICULO sin vender?","Artículos sin ventas en 180 días agrupados por proveedor.","SELECT COALESCE(P.NOMBRECOMERCIAL,P.RAZONSOCIAL) AS PROVEEDOR,COUNT(*) AS N_ARTICULOS,ROUND(SUM(A.STOCKARTICULO*A.PRECIOCOSTE),2) AS VALOR_INMOVILIZADO FROM ARTICULO A LEFT JOIN PROVEED P ON P.CODIGO=A.PROVEEDDEFECTO WHERE A.STOCKARTICULO>0 AND A.PRECIOCOSTE>0 AND A.CODIGO NOT IN (SELECT DISTINCT L.CODARTICULO FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.TIPO=13 AND D.FECHA>=DATE('now','-180 days')) GROUP BY A.PROVEEDDEFECTO,P.NOMBRECOMERCIAL,P.RAZONSOCIAL ORDER BY VALOR_INMOVILIZADO DESC LIMIT 20","Compras","Almacén","Proveedor","Alto","STOCKARTICULO inmovilizado",""),
    q("cx3_050","Dashboard de compras y almacén","¿Cuál es el estado completo de compras y almacén?","KPIs de compras: proveedores, artículos, STOCKARTICULO, valor inventario.","SELECT (SELECT COUNT(*) FROM PROVEED) AS N_PROVEEDORES,(SELECT COUNT(*) FROM ARTICULO) AS N_ARTICULOS,(SELECT COUNT(*) FROM ARTICULO WHERE STOCKARTICULO>0) AS CON_STOCKARTICULO,(SELECT ROUND(SUM(STOCKARTICULO*PRECIOCOSTE),2) FROM ARTICULO WHERE STOCKARTICULO>0 AND PRECIOCOSTE>0) AS VALOR_INVENTARIO,(SELECT COUNT(*) FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO<STOCKARTICULO) AS BAJO_MINIMO","Compras","Dirección","KPI","Crítico","Dashboard",""),
    q("cx3_051","Artículos con mayor número de líneas en albaranes de compra","¿Qué artículos se reciben más en albaranes?","Artículos en líneas de documentos TIPO=11 (albaranes).","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS,SUM(L.CANTIDAD) AS TOTAL_UNIDADES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=11 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Albaranes",""),
    q("cx3_052","Artículos con mayor número de líneas en pedidos","¿Qué artículos se piden más?","Artículos en líneas de documentos TIPO=12 (pedidos).","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS,SUM(L.CANTIDAD) AS TOTAL_UNIDADES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=12 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Pedidos",""),
    q("cx3_053","Artículos con mayor número de líneas en presupuestos","¿Qué artículos se presupuestan más?","Artículos en líneas de documentos TIPO=0 (presupuestos).","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS,SUM(L.CANTIDAD) AS TOTAL_UNIDADES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=0 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS DESC LIMIT 20","Compras","Comercial","Artículo","Bajo","Presupuestos",""),
    q("cx3_054","Artículos con mayor número de líneas en SATs","¿Qué artículos se usan más en SATs?","Artículos en líneas de documentos TIPO=2 (SATs).","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS,SUM(L.CANTIDAD) AS TOTAL_UNIDADES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=2 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS DESC LIMIT 20","Compras","SAT / Técnico","Artículo","Bajo","SAT",""),
    q("cx3_055","Artículos con mayor número de líneas en todos los documentos","¿Qué artículos aparecen en más documentos en total?","Artículos en líneas de todos los tipos de documento.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_TOTAL,COUNT(DISTINCT D.TIPO) AS N_TIPOS_DOC FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_TOTAL DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_056","Artículos con PRECIOVENTA de PRECIOCOSTE entre 0 y 10€","¿Cuántos artículos tienen PRECIOCOSTE bajo (0-10€)?","Artículos con PRECIOCOSTE entre 0.01 y 10.","SELECT COUNT(*) AS N_ARTICULOS,ROUND(SUM(STOCKARTICULO),2) AS TOTAL_UNIDADES,ROUND(SUM(STOCKARTICULO*PRECIOCOSTE),2) AS VALOR_TOTAL FROM ARTICULO WHERE PRECIOCOSTE>0 AND PRECIOCOSTE<=10","Compras","Almacén","Artículo","Bajo","Costes",""),
    q("cx3_057","Artículos con PRECIOVENTA de PRECIOCOSTE entre 10 y 100€","¿Cuántos artículos tienen PRECIOCOSTE medio (10-100€)?","Artículos con PRECIOCOSTE entre 10 y 100.","SELECT COUNT(*) AS N_ARTICULOS,ROUND(SUM(STOCKARTICULO),2) AS TOTAL_UNIDADES,ROUND(SUM(STOCKARTICULO*PRECIOCOSTE),2) AS VALOR_TOTAL FROM ARTICULO WHERE PRECIOCOSTE>10 AND PRECIOCOSTE<=100","Compras","Almacén","Artículo","Bajo","Costes",""),
    q("cx3_058","Artículos con PRECIOVENTA de PRECIOCOSTE superior a 100€","¿Cuántos artículos tienen PRECIOCOSTE alto (>100€)?","Artículos con PRECIOCOSTE>100.","SELECT COUNT(*) AS N_ARTICULOS,ROUND(SUM(STOCKARTICULO),2) AS TOTAL_UNIDADES,ROUND(SUM(STOCKARTICULO*PRECIOCOSTE),2) AS VALOR_TOTAL FROM ARTICULO WHERE PRECIOCOSTE>100","Compras","Almacén","Artículo","Bajo","Costes",""),
    q("cx3_059","Artículos con PRECIOVENTA de PRECIOCOSTE superior a 1000€","¿Cuántos artículos tienen PRECIOCOSTE muy alto (>1000€)?","Artículos con PRECIOCOSTE>1000.","SELECT CODIGO,NOMBRE,ROUND(PRECIOCOSTE,2) AS COSTE,ROUND(PRECIOVENTA,2) AS PVP,STOCKARTICULO AS STOCKARTICULO FROM ARTICULO WHERE PRECIOCOSTE>1000 ORDER BY PRECIOCOSTE DESC LIMIT 20","Compras","Dirección","Artículo","Medio","Costes",""),
    q("cx3_060","Artículos con mayor valor de STOCKARTICULO por unidad","¿Qué artículos tienen mayor valor unitario de STOCKARTICULO?","Artículos con mayor PRECIOCOSTE*STOCKARTICULO/STOCKARTICULO = PRECIOCOSTE.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,ROUND(PRECIOCOSTE,2) AS COSTE_UNITARIO,ROUND(STOCKARTICULO*PRECIOCOSTE,2) AS VALOR_TOTAL FROM ARTICULO WHERE STOCKARTICULO>0 AND PRECIOCOSTE>0 ORDER BY COSTE_UNITARIO DESC LIMIT 20","Compras","Almacén","Artículo","Medio","Inventario",""),
    q("cx3_061","Artículos con mayor diferencia entre STOCKARTICULO máximo y mínimo","¿Qué artículos tienen mayor rango de STOCKARTICULO configurado?","Diferencia STOCKARTICULO-STOCKARTICULO por artículo.","SELECT CODIGO,NOMBRE,STOCKARTICULO,STOCKARTICULO,STOCKARTICULO-STOCKARTICULO AS RANGO,STOCKARTICULO AS STOCK_ACTUAL FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO>0 ORDER BY RANGO DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","STOCKARTICULO",""),
    q("cx3_062","Artículos con STOCKARTICULO exactamente igual al mínimo","¿Qué artículos están justo en el límite mínimo?","Artículos con STOCKARTICULO=STOCKARTICULO.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,STOCKARTICULO AS MINIMO FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO=STOCKARTICULO ORDER BY NOMBRE LIMIT 20","Compras","Almacén","Artículo","Medio","STOCKARTICULO",""),
    q("cx3_063","Artículos con STOCKARTICULO exactamente igual al máximo","¿Qué artículos están justo en el límite máximo?","Artículos con STOCKARTICULO=STOCKARTICULO.","SELECT CODIGO,NOMBRE,STOCKARTICULO AS STOCKARTICULO,STOCKARTICULO AS MAXIMO FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO=STOCKARTICULO ORDER BY NOMBRE LIMIT 20","Compras","Almacén","Artículo","Bajo","STOCKARTICULO",""),
    q("cx3_064","Artículos con mayor número de equivalencias","¿Qué artículos tienen más sustitutos?","Artículos con más registros en EQUIVAL.","SELECT A.CODIGO,A.NOMBRE,COUNT(*) AS N_EQUIVALENCIAS FROM ARTICULO LEFT JOIN ARTICULO A ON A.CODIGO=A.CODIGO GROUP BY A.CODIGO,A.NOMBRE ORDER BY N_EQUIVALENCIAS DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Equivalencias",""),
    q("cx3_065","Artículos sin equivalencias registradas","¿Cuántos artículos no tienen sustitutos?","Artículos sin registros en EQUIVAL.","SELECT COUNT(*) AS N_SIN_EQUIVALENCIAS FROM ARTICULO WHERE CODIGO NOT IN (SELECT DISTINCT CODIGO FROM EQUIVAL)","Compras","Almacén","KPI","Bajo","Equivalencias",""),
    q("cx3_066","Artículos con mayor número de documentos asociados","¿Qué artículos tienen más documentos en DOCLINDOCASOC?","Artículos con más registros en DOCLINDOCASOC.","SELECT CODIGO,COUNT(*) AS N_DOCS FROM DOCLIN GROUP BY CODIGO ORDER BY N_DOCS DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Documentos",""),
    q("cx3_067","Artículos con mayor número de líneas en todos los tipos de documento","¿Qué artículos son más transaccionales?","Artículos con más líneas en DOCLIN de cualquier tipo.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_068","Artículos con mayor valor de ventas en el año","¿Qué artículos generan más ingresos en el año actual?","Suma de PRECIO*CANTIDAD por artículo en facturas TIPO=13 del año actual.","SELECT L.CODARTICULO,A.NOMBRE,ROUND(SUM(L.PRECIO*L.CANTIDAD),2) AS INGRESOS_ANIO FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=13 AND SUBSTR(D.FECHA,1,4)=SUBSTR(DATE('now'),1,4) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY INGRESOS_ANIO DESC LIMIT 20","Compras","Dirección","Artículo","Alto","Ingresos",""),
    q("cx3_069","Artículos con mayor PRECIOCOSTE de ventas en el año","¿Qué artículos tienen mayor COGS en el año actual?","Suma de PRECIOCOSTE*CANTIDAD por artículo en facturas TIPO=13 del año actual.","SELECT L.CODARTICULO,A.NOMBRE,ROUND(SUM(A.PRECIOCOSTE*L.CANTIDAD),2) AS COGS_ANIO FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=13 AND A.PRECIOCOSTE>0 AND SUBSTR(D.FECHA,1,4)=SUBSTR(DATE('now'),1,4) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY COGS_ANIO DESC LIMIT 20","Compras","Dirección","Artículo","Alto","COGS",""),
    q("cx3_070","Artículos con mayor margen total en el año","¿Qué artículos generan más margen en el año actual?","Suma de (PRECIO-PRECIOCOSTE)*CANTIDAD por artículo en facturas TIPO=13 del año actual.","SELECT L.CODARTICULO,A.NOMBRE,ROUND(SUM((L.PRECIO-A.PRECIOCOSTE)*L.CANTIDAD),2) AS MARGEN_ANIO FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=13 AND A.PRECIOCOSTE>0 AND SUBSTR(D.FECHA,1,4)=SUBSTR(DATE('now'),1,4) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY MARGEN_ANIO DESC LIMIT 20","Compras","Dirección","Artículo","Alto","Margen",""),
    q("cx3_071","Artículos con mayor número de clientes distintos en el año","¿Qué artículos compran más clientes distintos en el año?","Clientes únicos por artículo en facturas TIPO=13 del año actual.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(DISTINCT D.CODCLIENTE) AS N_CLIENTES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=13 AND SUBSTR(D.FECHA,1,4)=SUBSTR(DATE('now'),1,4) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_CLIENTES DESC LIMIT 20","Compras","Comercial","Artículo","Medio","Popularidad",""),
    q("cx3_072","Artículos con mayor número de unidades vendidas en el año","¿Qué artículos se venden más en unidades en el año?","Suma de CANTIDAD por artículo en facturas TIPO=13 del año actual.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS UNIDADES_ANIO FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=13 AND SUBSTR(D.FECHA,1,4)=SUBSTR(DATE('now'),1,4) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY UNIDADES_ANIO DESC LIMIT 20","Compras","Comercial","Artículo","Medio","Ventas",""),
    q("cx3_073","Artículos con mayor número de unidades vendidas en el mes","¿Qué artículos se venden más en unidades este mes?","Suma de CANTIDAD por artículo en facturas TIPO=13 del mes actual.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS UNIDADES_MES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=13 AND SUBSTR(D.FECHA,1,7)=SUBSTR(DATE('now'),1,7) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY UNIDADES_MES DESC LIMIT 20","Compras","Comercial","Artículo","Medio","Ventas",""),
    q("cx3_074","Artículos con mayor número de unidades en STOCKARTICULO por familia","¿Qué familia tiene más unidades en STOCKARTICULO?","Suma de STOCKARTICULO por familia.","SELECT A.CODFAMILIA,F.NOMBRE AS FAMILIA,SUM(A.STOCKARTICULO) AS TOTAL_UNIDADES FROM ARTICULO A LEFT JOIN FAMILIA F ON F.CODIGO=A.CODFAMILIA WHERE A.STOCKARTICULO>0 GROUP BY A.CODFAMILIA,F.NOMBRE ORDER BY TOTAL_UNIDADES DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Inventario",""),
    q("cx3_075","Artículos con mayor número de unidades vendidas por familia","¿Qué familia se vende más en unidades?","Suma de CANTIDAD por familia en facturas TIPO=13.","SELECT A.CODFAMILIA,F.NOMBRE AS FAMILIA,SUM(L.CANTIDAD) AS TOTAL_UNIDADES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO LEFT JOIN FAMILIA F ON F.CODIGO=A.CODFAMILIA WHERE D.TIPO=13 GROUP BY A.CODFAMILIA,F.NOMBRE ORDER BY TOTAL_UNIDADES DESC LIMIT 20","Compras","Comercial","Artículo","Medio","Ventas",""),
    q("cx3_076","Artículos con mayor número de unidades en pedidos pendientes","¿Qué artículos tienen más unidades en pedidos sin servir?","Suma de CANTIDAD en líneas de pedidos TIPO=12 sin conversión.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS UNIDADES_PENDIENTES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN DOCDESTINO DD ON DD.CODDOCUMENTO=D.CODIGO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=12 AND DD.CODDOCUMENTO IS NULL GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY UNIDADES_PENDIENTES DESC LIMIT 20","Compras","Almacén","Artículo","Alto","Pedidos",""),
    q("cx3_077","Artículos con mayor número de unidades en albaranes pendientes","¿Qué artículos tienen más unidades en albaranes sin facturar?","Suma de CANTIDAD en líneas de albaranes TIPO=11 sin conversión.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS UNIDADES_PENDIENTES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN DOCDESTINO DD ON DD.CODDOCUMENTO=D.CODIGO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=11 AND DD.CODDOCUMENTO IS NULL GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY UNIDADES_PENDIENTES DESC LIMIT 20","Compras","Almacén","Artículo","Medio","Albaranes",""),
    q("cx3_078","Artículos con mayor número de unidades en presupuestos pendientes","¿Qué artículos tienen más unidades en presupuestos sin convertir?","Suma de CANTIDAD en líneas de presupuestos TIPO=0 sin conversión.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS UNIDADES_PRESUPUESTADAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN DOCDESTINO DD ON DD.CODDOCUMENTO=D.CODIGO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=0 AND DD.CODDOCUMENTO IS NULL GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY UNIDADES_PRESUPUESTADAS DESC LIMIT 20","Compras","Comercial","Artículo","Medio","Pipeline",""),
    q("cx3_079","Artículos con mayor número de unidades en SATs","¿Qué artículos se usan más en SATs?","Suma de CANTIDAD en líneas de SATs TIPO=2.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS UNIDADES_SAT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=2 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY UNIDADES_SAT DESC LIMIT 20","Compras","SAT / Técnico","Artículo","Bajo","SAT",""),
    q("cx3_080","Artículos con mayor número de unidades en contratos","¿Qué artículos se incluyen más en contratos?","Suma de CANTIDAD en líneas de contratos TIPO=10.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS UNIDADES_CONTRATO FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=10 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY UNIDADES_CONTRATO DESC LIMIT 20","Compras","Comercial","Artículo","Bajo","Contratos",""),
    q("cx3_081","Artículos con mayor número de unidades en certificaciones","¿Qué artículos se incluyen más en certificaciones?","Suma de CANTIDAD en líneas de certificaciones TIPO=51.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS UNIDADES_CERT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=51 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY UNIDADES_CERT DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Certificaciones",""),
    q("cx3_082","Artículos con mayor número de unidades en recibos","¿Qué artículos se incluyen más en recibos?","Suma de CANTIDAD en líneas de recibos TIPO=61.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS UNIDADES_RECIBO FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=61 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY UNIDADES_RECIBO DESC LIMIT 20","Compras","Finanzas","Artículo","Bajo","Recibos",""),
    q("cx3_083","Artículos con mayor número de unidades en abonos","¿Qué artículos se devuelven más?","Suma de CANTIDAD en líneas de abonos TIPO=3.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS UNIDADES_ABONO FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=3 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY UNIDADES_ABONO DESC LIMIT 20","Compras","Comercial","Artículo","Medio","Devoluciones",""),
    q("cx3_084","Artículos con mayor número de unidades en todos los documentos","¿Qué artículos tienen más movimiento total?","Suma de CANTIDAD en DOCLIN de todos los tipos.","SELECT L.CODARTICULO,A.NOMBRE,SUM(L.CANTIDAD) AS TOTAL_UNIDADES FROM DOCLIN L LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY TOTAL_UNIDADES DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_085","Artículos con mayor PRECIOVENTA de venta","¿Qué artículos tienen mayor PRECIOVENTA de venta en tarifa?","Top 20 artículos por PRECIOVENTA.","SELECT CODIGO,NOMBRE,ROUND(PRECIOVENTA,2) AS PVP,ROUND(PRECIOCOSTE,2) AS COSTE,STOCKARTICULO AS STOCKARTICULO FROM ARTICULO WHERE PRECIOVENTA>0 ORDER BY PRECIOVENTA DESC LIMIT 20","Compras","Comercial","Artículo","Bajo","Precios",""),
    q("cx3_086","Artículos con menor PRECIOVENTA de venta (no cero)","¿Qué artículos tienen menor PRECIOVENTA de venta?","Top 20 artículos por PRECIOVENTA ascendente (excluyendo cero).","SELECT CODIGO,NOMBRE,ROUND(PRECIOVENTA,2) AS PVP,ROUND(PRECIOCOSTE,2) AS COSTE FROM ARTICULO WHERE PRECIOVENTA>0 ORDER BY PRECIOVENTA ASC LIMIT 20","Compras","Comercial","Artículo","Bajo","Precios",""),
    q("cx3_087","Artículos con mayor número de familias distintas vendidas juntas","¿Qué familias se venden juntas con más frecuencia?","Pares de familias que aparecen en la misma factura TIPO=13.","SELECT A1.CODFAMILIA AS FAM1,F1.NOMBRE AS NOMBRE_FAM1,A2.CODFAMILIA AS FAM2,F2.NOMBRE AS NOMBRE_FAM2,COUNT(*) AS N_FACTURAS FROM DOCLIN L1 JOIN DOCLIN L2 ON L2.CODDOCUMENTO=L1.CODDOCUMENTO AND L2.CODIGO>L1.CODIGO JOIN DOCCAB D ON D.CODIGO=L1.CODDOCUMENTO LEFT JOIN ARTICULO A1 ON A1.CODIGO=L1.CODIGO LEFT JOIN ARTICULO A2 ON A2.CODIGO=L2.CODIGO LEFT JOIN FAMILIA F1 ON F1.CODIGO=A1.CODFAMILIA LEFT JOIN FAMILIA F2 ON F2.CODIGO=A2.CODFAMILIA WHERE D.TIPO=13 AND A1.CODFAMILIA IS NOT NULL AND A2.CODFAMILIA IS NOT NULL AND A1.CODFAMILIA<>A2.CODFAMILIA GROUP BY A1.CODFAMILIA,A2.CODFAMILIA,F1.NOMBRE,F2.NOMBRE ORDER BY N_FACTURAS DESC LIMIT 20","Compras","Comercial","Artículo","Bajo","Cross-selling",""),
    q("cx3_088","Artículos con mayor número de proveedores distintos (via EQUIVAL)","¿Qué artículos tienen más proveedores alternativos?","Artículos con más equivalencias en EQUIVAL.","SELECT A.CODIGO,A.NOMBRE,COUNT(*) AS N_EQUIV FROM ARTICULO LEFT JOIN ARTICULO A ON A.CODIGO=A.CODIGO GROUP BY A.CODIGO,A.NOMBRE HAVING COUNT(*)>1 ORDER BY N_EQUIV DESC LIMIT 20","Compras","Compras","Artículo","Bajo","Equivalencias",""),
    q("cx3_089","Artículos con mayor número de líneas en documentos de SAT","¿Qué artículos se usan más en servicios técnicos?","Artículos en líneas de SATs TIPO=2 con más frecuencia.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_SAT,SUM(L.CANTIDAD) AS CANTIDAD FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=2 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_SAT DESC LIMIT 20","Compras","SAT / Técnico","Artículo","Bajo","SAT",""),
    q("cx3_090","Artículos con mayor número de líneas en documentos de ventas y SAT","¿Qué artículos se venden tanto en ventas como en SAT?","Artículos con líneas en TIPO=13 y TIPO=2.","SELECT L.CODARTICULO,A.NOMBRE,SUM(CASE WHEN D.TIPO=13 THEN 1 ELSE 0 END) AS N_VENTAS,SUM(CASE WHEN D.TIPO=2 THEN 1 ELSE 0 END) AS N_SAT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO IN (13,2) GROUP BY L.CODARTICULO,A.NOMBRE HAVING N_VENTAS>0 AND N_SAT>0 ORDER BY N_VENTAS+N_SAT DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_091","Artículos con mayor número de líneas en documentos de ventas y presupuestos","¿Qué artículos se presupuestan y venden más?","Artículos con líneas en TIPO=0 y TIPO=13.","SELECT L.CODARTICULO,A.NOMBRE,SUM(CASE WHEN D.TIPO=0 THEN 1 ELSE 0 END) AS N_PRESUPUESTOS,SUM(CASE WHEN D.TIPO=13 THEN 1 ELSE 0 END) AS N_FACTURAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO IN (0,13) GROUP BY L.CODARTICULO,A.NOMBRE HAVING N_PRESUPUESTOS>0 AND N_FACTURAS>0 ORDER BY N_FACTURAS DESC LIMIT 20","Compras","Comercial","Artículo","Bajo","Popularidad",""),
    q("cx3_092","Artículos con mayor número de líneas en documentos de ventas y albaranes","¿Qué artículos se albaranan y facturan más?","Artículos con líneas en TIPO=11 y TIPO=13.","SELECT L.CODARTICULO,A.NOMBRE,SUM(CASE WHEN D.TIPO=11 THEN 1 ELSE 0 END) AS N_ALBARANES,SUM(CASE WHEN D.TIPO=13 THEN 1 ELSE 0 END) AS N_FACTURAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO IN (11,13) GROUP BY L.CODARTICULO,A.NOMBRE HAVING N_ALBARANES>0 AND N_FACTURAS>0 ORDER BY N_FACTURAS DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Popularidad",""),
    q("cx3_093","Artículos con mayor número de líneas en documentos de ventas y pedidos","¿Qué artículos se piden y facturan más?","Artículos con líneas en TIPO=12 y TIPO=13.","SELECT L.CODARTICULO,A.NOMBRE,SUM(CASE WHEN D.TIPO=12 THEN 1 ELSE 0 END) AS N_PEDIDOS,SUM(CASE WHEN D.TIPO=13 THEN 1 ELSE 0 END) AS N_FACTURAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO IN (12,13) GROUP BY L.CODARTICULO,A.NOMBRE HAVING N_PEDIDOS>0 AND N_FACTURAS>0 ORDER BY N_FACTURAS DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Popularidad",""),
    q("cx3_094","Artículos con mayor número de líneas en documentos de ventas y abonos","¿Qué artículos se venden y devuelven más?","Artículos con líneas en TIPO=13 y TIPO=3.","SELECT L.CODARTICULO,A.NOMBRE,SUM(CASE WHEN D.TIPO=13 THEN 1 ELSE 0 END) AS N_FACTURAS,SUM(CASE WHEN D.TIPO=3 THEN 1 ELSE 0 END) AS N_ABONOS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO IN (13,3) GROUP BY L.CODARTICULO,A.NOMBRE HAVING N_ABONOS>0 ORDER BY N_ABONOS DESC LIMIT 20","Compras","Comercial","Artículo","Medio","Devoluciones",""),
    q("cx3_095","Artículos con mayor número de líneas en documentos de ventas y contratos","¿Qué artículos se contratan y facturan más?","Artículos con líneas en TIPO=10 y TIPO=13.","SELECT L.CODARTICULO,A.NOMBRE,SUM(CASE WHEN D.TIPO=10 THEN 1 ELSE 0 END) AS N_CONTRATOS,SUM(CASE WHEN D.TIPO=13 THEN 1 ELSE 0 END) AS N_FACTURAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO IN (10,13) GROUP BY L.CODARTICULO,A.NOMBRE HAVING N_CONTRATOS>0 AND N_FACTURAS>0 ORDER BY N_FACTURAS DESC LIMIT 20","Compras","Comercial","Artículo","Bajo","Contratos",""),
    q("cx3_096","Artículos con mayor número de líneas en documentos de ventas y certificaciones","¿Qué artículos se certifican y facturan más?","Artículos con líneas en TIPO=51 y TIPO=13.","SELECT L.CODARTICULO,A.NOMBRE,SUM(CASE WHEN D.TIPO=51 THEN 1 ELSE 0 END) AS N_CERT,SUM(CASE WHEN D.TIPO=13 THEN 1 ELSE 0 END) AS N_FACTURAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO IN (51,13) GROUP BY L.CODARTICULO,A.NOMBRE HAVING N_CERT>0 AND N_FACTURAS>0 ORDER BY N_FACTURAS DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Certificaciones",""),
    q("cx3_097","Artículos con mayor número de líneas en documentos de ventas y recibos","¿Qué artículos se incluyen en recibos y facturas?","Artículos con líneas en TIPO=61 y TIPO=13.","SELECT L.CODARTICULO,A.NOMBRE,SUM(CASE WHEN D.TIPO=61 THEN 1 ELSE 0 END) AS N_RECIBOS,SUM(CASE WHEN D.TIPO=13 THEN 1 ELSE 0 END) AS N_FACTURAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO IN (61,13) GROUP BY L.CODARTICULO,A.NOMBRE HAVING N_RECIBOS>0 AND N_FACTURAS>0 ORDER BY N_FACTURAS DESC LIMIT 20","Compras","Finanzas","Artículo","Bajo","Recibos",""),
    q("cx3_098","Artículos con mayor número de líneas en todos los documentos de venta","¿Qué artículos son más transaccionales en ventas?","Artículos con más líneas en TIPO=13 en todo el histórico.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_VENTA FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=13 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_VENTA DESC LIMIT 20","Compras","Comercial","Artículo","Bajo","Popularidad",""),
    q("cx3_099","Artículos con mayor número de líneas en todos los documentos de compra","¿Qué artículos son más transaccionales en compras?","Artículos con más líneas en documentos que no son TIPO=13.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_COMPRA FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO NOT IN (13) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_COMPRA DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Popularidad",""),
    q("cx3_100","Artículos con mayor número de líneas en todos los documentos de SAT","¿Qué artículos son más transaccionales en SAT?","Artículos con más líneas en TIPO=2 en todo el histórico.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_SAT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=2 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_SAT DESC LIMIT 20","Compras","SAT / Técnico","Artículo","Bajo","SAT",""),
    q("cx3_101","Artículos con mayor número de líneas en todos los documentos de presupuestos","¿Qué artículos son más transaccionales en presupuestos?","Artículos con más líneas en TIPO=0 en todo el histórico.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_PRESUP FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=0 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_PRESUP DESC LIMIT 20","Compras","Comercial","Artículo","Bajo","Presupuestos",""),
    q("cx3_102","Artículos con mayor número de líneas en todos los documentos de albaranes","¿Qué artículos son más transaccionales en albaranes?","Artículos con más líneas en TIPO=11 en todo el histórico.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_ALB FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=11 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_ALB DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Albaranes",""),
    q("cx3_103","Artículos con mayor número de líneas en todos los documentos de pedidos","¿Qué artículos son más transaccionales en pedidos?","Artículos con más líneas en TIPO=12 en todo el histórico.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_PED FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=12 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_PED DESC LIMIT 20","Compras","Almacén","Artículo","Bajo","Pedidos",""),
    q("cx3_104","Artículos con mayor número de líneas en todos los documentos de abonos","¿Qué artículos son más transaccionales en abonos?","Artículos con más líneas en TIPO=3 en todo el histórico.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_ABONO FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=3 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_ABONO DESC LIMIT 20","Compras","Comercial","Artículo","Bajo","Devoluciones",""),
    q("cx3_105","Artículos con mayor número de líneas en todos los documentos de contratos","¿Qué artículos son más transaccionales en contratos?","Artículos con más líneas en TIPO=10 en todo el histórico.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_CONT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=10 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_CONT DESC LIMIT 20","Compras","Comercial","Artículo","Bajo","Contratos",""),
    q("cx3_106","Artículos con mayor número de líneas en todos los documentos de certificaciones","¿Qué artículos son más transaccionales en certificaciones?","Artículos con más líneas en TIPO=51 en todo el histórico.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_CERT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=51 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_CERT DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Certificaciones",""),
    q("cx3_107","Artículos con mayor número de líneas en todos los documentos de recibos","¿Qué artículos son más transaccionales en recibos?","Artículos con más líneas en TIPO=61 en todo el histórico.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_REC FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.TIPO=61 GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_REC DESC LIMIT 20","Compras","Finanzas","Artículo","Bajo","Recibos",""),
    q("cx3_108","Artículos con mayor número de líneas en todos los documentos de todos los tipos","¿Qué artículos son más transaccionales en total?","Artículos con más líneas en DOCLIN de todos los tipos.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_TOTAL FROM DOCLIN L LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_TOTAL DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_109","Artículos con mayor número de líneas en todos los documentos de todos los tipos por mes","¿Qué artículos son más transaccionales este mes?","Artículos con más líneas en DOCLIN del mes actual.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_MES FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE SUBSTR(D.FECHA,1,7)=SUBSTR(DATE('now'),1,7) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_MES DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_110","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año","¿Qué artículos son más transaccionales en el año actual?","Artículos con más líneas en DOCLIN del año actual.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_ANIO FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE SUBSTR(D.FECHA,1,4)=SUBSTR(DATE('now'),1,4) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_ANIO DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_111","Artículos con mayor número de líneas en todos los documentos de todos los tipos por trimestre","¿Qué artículos son más transaccionales en el trimestre actual?","Artículos con más líneas en DOCLIN del trimestre actual.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_TRIM FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-90 days') GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_TRIM DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_112","Artículos con mayor número de líneas en todos los documentos de todos los tipos por semestre","¿Qué artículos son más transaccionales en el semestre actual?","Artículos con más líneas en DOCLIN del semestre actual.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_SEM FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-180 days') GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_SEM DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_113","Artículos con mayor número de líneas en todos los documentos de todos los tipos por semana","¿Qué artículos son más transaccionales esta semana?","Artículos con más líneas en DOCLIN de los últimos 7 días.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_SEM FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-7 days') GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_SEM DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_114","Artículos con mayor número de líneas en todos los documentos de todos los tipos por día","¿Qué artículos son más transaccionales hoy?","Artículos con más líneas en DOCLIN de hoy.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_HOY FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA=DATE('now') GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_HOY DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_115","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año anterior","¿Qué artículos fueron más transaccionales el año anterior?","Artículos con más líneas en DOCLIN del año anterior.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_ANIO_ANT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE SUBSTR(D.FECHA,1,4)=CAST(CAST(SUBSTR(DATE('now'),1,4) AS INTEGER)-1 AS TEXT) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_ANIO_ANT DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_116","Artículos con mayor número de líneas en todos los documentos de todos los tipos por mes anterior","¿Qué artículos fueron más transaccionales el mes anterior?","Artículos con más líneas en DOCLIN del mes anterior.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_MES_ANT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE SUBSTR(D.FECHA,1,7)=SUBSTR(DATE('now','-1 month'),1,7) GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_MES_ANT DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_117","Artículos con mayor número de líneas en todos los documentos de todos los tipos por trimestre anterior","¿Qué artículos fueron más transaccionales el trimestre anterior?","Artículos con más líneas en DOCLIN del trimestre anterior.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_TRIM_ANT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-180 days') AND D.FECHA<DATE('now','-90 days') GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_TRIM_ANT DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_118","Artículos con mayor número de líneas en todos los documentos de todos los tipos por semestre anterior","¿Qué artículos fueron más transaccionales el semestre anterior?","Artículos con más líneas en DOCLIN del semestre anterior.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_SEM_ANT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-365 days') AND D.FECHA<DATE('now','-180 days') GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_SEM_ANT DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_119","Artículos con mayor número de líneas en todos los documentos de todos los tipos por semana anterior","¿Qué artículos fueron más transaccionales la semana anterior?","Artículos con más líneas en DOCLIN de la semana anterior.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_SEM_ANT FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-14 days') AND D.FECHA<DATE('now','-7 days') GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_SEM_ANT DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_120","Artículos con mayor número de líneas en todos los documentos de todos los tipos por día anterior","¿Qué artículos fueron más transaccionales ayer?","Artículos con más líneas en DOCLIN de ayer.","SELECT L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS_AYER FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA=DATE('now','-1 day') GROUP BY L.CODARTICULO,A.NOMBRE ORDER BY N_LINEAS_AYER DESC LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_121","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes","¿Qué artículos son más transaccionales por año y mes?","Artículos con más líneas en DOCLIN por año y mes.","SELECT SUBSTR(D.FECHA,1,7) AS MES,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-3 months') GROUP BY SUBSTR(D.FECHA,1,7),L.CODARTICULO,A.NOMBRE ORDER BY MES DESC,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_122","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y trimestre","¿Qué artículos son más transaccionales por año y trimestre?","Artículos con más líneas en DOCLIN por año y trimestre.","SELECT SUBSTR(D.FECHA,1,4) AS ANIO,CASE WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 1 AND 3 THEN 'Q1' WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 4 AND 6 THEN 'Q2' WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 7 AND 9 THEN 'Q3' ELSE 'Q4' END AS TRIMESTRE,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-1 year') GROUP BY ANIO,TRIMESTRE,L.CODARTICULO,A.NOMBRE ORDER BY ANIO DESC,TRIMESTRE,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_123","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y semestre","¿Qué artículos son más transaccionales por año y semestre?","Artículos con más líneas en DOCLIN por año y semestre.","SELECT SUBSTR(D.FECHA,1,4) AS ANIO,CASE WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER)<=6 THEN 'H1' ELSE 'H2' END AS SEMESTRE,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-2 years') GROUP BY ANIO,SEMESTRE,L.CODARTICULO,A.NOMBRE ORDER BY ANIO DESC,SEMESTRE,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_124","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y semana","¿Qué artículos son más transaccionales por semana?","Artículos con más líneas en DOCLIN por semana.","SELECT STRFTIME('%Y-W%W',D.FECHA) AS SEMANA,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-4 weeks') GROUP BY SEMANA,L.CODARTICULO,A.NOMBRE ORDER BY SEMANA DESC,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_125","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y día","¿Qué artículos son más transaccionales por día?","Artículos con más líneas en DOCLIN por día en los últimos 7 días.","SELECT D.FECHA,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA>=DATE('now','-7 days') GROUP BY D.FECHA,L.CODARTICULO,A.NOMBRE ORDER BY D.FECHA DESC,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_126","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y hora","¿Qué artículos son más transaccionales por hora del día?","Artículos con más líneas en DOCLIN por hora (si hay timestamp).","SELECT SUBSTR(D.FECHA,12,2) AS HORA,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE LENGTH(D.FECHA)>10 GROUP BY HORA,L.CODARTICULO,A.NOMBRE ORDER BY HORA,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_127","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y día de la semana","¿Qué artículos son más transaccionales por día de la semana?","Artículos con más líneas en DOCLIN por día de la semana.","SELECT CAST(STRFTIME('%w',D.FECHA) AS INTEGER) AS DIA_SEMANA,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA IS NOT NULL GROUP BY DIA_SEMANA,L.CODARTICULO,A.NOMBRE ORDER BY DIA_SEMANA,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_128","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes del año","¿Qué artículos son más transaccionales por mes del año?","Artículos con más líneas en DOCLIN por mes del año (1-12).","SELECT CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) AS MES_NUM,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA IS NOT NULL GROUP BY MES_NUM,L.CODARTICULO,A.NOMBRE ORDER BY MES_NUM,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_129","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y trimestre del año","¿Qué artículos son más transaccionales por trimestre del año?","Artículos con más líneas en DOCLIN por trimestre del año.","SELECT CASE WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 1 AND 3 THEN 'Q1' WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 4 AND 6 THEN 'Q2' WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 7 AND 9 THEN 'Q3' ELSE 'Q4' END AS TRIMESTRE,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA IS NOT NULL GROUP BY TRIMESTRE,L.CODARTICULO,A.NOMBRE ORDER BY TRIMESTRE,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_130","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y semestre del año","¿Qué artículos son más transaccionales por semestre del año?","Artículos con más líneas en DOCLIN por semestre del año.","SELECT CASE WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER)<=6 THEN 'H1' ELSE 'H2' END AS SEMESTRE,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA IS NOT NULL GROUP BY SEMESTRE,L.CODARTICULO,A.NOMBRE ORDER BY SEMESTRE,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_131","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y semana del año","¿Qué artículos son más transaccionales por semana del año?","Artículos con más líneas en DOCLIN por semana del año.","SELECT STRFTIME('%W',D.FECHA) AS SEMANA_NUM,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA IS NOT NULL GROUP BY SEMANA_NUM,L.CODARTICULO,A.NOMBRE ORDER BY SEMANA_NUM,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_132","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y día del año","¿Qué artículos son más transaccionales por día del año?","Artículos con más líneas en DOCLIN por día del año.","SELECT STRFTIME('%j',D.FECHA) AS DIA_ANIO,L.CODARTICULO,A.NOMBRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO LEFT JOIN ARTICULO A ON A.CODIGO=L.CODARTICULO WHERE D.FECHA IS NOT NULL GROUP BY DIA_ANIO,L.CODARTICULO,A.NOMBRE ORDER BY DIA_ANIO,N_LINEAS DESC LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_133","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y hora del día","¿Qué artículos son más transaccionales por hora del día?","Artículos con más líneas en DOCLIN por hora del día (si hay timestamp).","SELECT SUBSTR(D.FECHA,12,2) AS HORA,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS_DISTINTOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE LENGTH(D.FECHA)>10 GROUP BY HORA ORDER BY HORA LIMIT 24","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_134","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y día de la semana y hora","¿Qué artículos son más transaccionales por día de la semana y hora?","Artículos con más líneas en DOCLIN por día de la semana y hora.","SELECT CAST(STRFTIME('%w',D.FECHA) AS INTEGER) AS DIA_SEMANA,SUBSTR(D.FECHA,12,2) AS HORA,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE LENGTH(D.FECHA)>10 GROUP BY DIA_SEMANA,HORA ORDER BY DIA_SEMANA,HORA LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_135","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y día","¿Qué artículos son más transaccionales por mes y día?","Artículos con más líneas en DOCLIN por mes y día del mes.","SELECT CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) AS MES,CAST(SUBSTR(D.FECHA,9,2) AS INTEGER) AS DIA,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY MES,DIA ORDER BY MES,DIA LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_136","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y semana","¿Qué artículos son más transaccionales por mes y semana?","Artículos con más líneas en DOCLIN por mes y semana del mes.","SELECT CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) AS MES,CAST((CAST(SUBSTR(D.FECHA,9,2) AS INTEGER)-1)/7 AS INTEGER)+1 AS SEMANA_MES,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY MES,SEMANA_MES ORDER BY MES,SEMANA_MES LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_137","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y trimestre","¿Qué artículos son más transaccionales por mes y trimestre?","Artículos con más líneas en DOCLIN por mes y trimestre.","SELECT CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) AS MES,CASE WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 1 AND 3 THEN 'Q1' WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 4 AND 6 THEN 'Q2' WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 7 AND 9 THEN 'Q3' ELSE 'Q4' END AS TRIMESTRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY MES,TRIMESTRE ORDER BY MES LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_138","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y semestre","¿Qué artículos son más transaccionales por mes y semestre?","Artículos con más líneas en DOCLIN por mes y semestre.","SELECT CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) AS MES,CASE WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER)<=6 THEN 'H1' ELSE 'H2' END AS SEMESTRE,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY MES,SEMESTRE ORDER BY MES LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_139","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año","¿Qué artículos son más transaccionales por mes y año?","Artículos con más líneas en DOCLIN por mes y año.","SELECT SUBSTR(D.FECHA,1,7) AS MES_ANIO,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS_DISTINTOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY MES_ANIO ORDER BY MES_ANIO DESC LIMIT 24","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_140","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año y trimestre","¿Qué artículos son más transaccionales por mes, año y trimestre?","Artículos con más líneas en DOCLIN por mes, año y trimestre.","SELECT SUBSTR(D.FECHA,1,4) AS ANIO,CASE WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 1 AND 3 THEN 'Q1' WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 4 AND 6 THEN 'Q2' WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER) BETWEEN 7 AND 9 THEN 'Q3' ELSE 'Q4' END AS TRIMESTRE,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY ANIO,TRIMESTRE ORDER BY ANIO DESC,TRIMESTRE LIMIT 30","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_141","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año y semestre","¿Qué artículos son más transaccionales por mes, año y semestre?","Artículos con más líneas en DOCLIN por mes, año y semestre.","SELECT SUBSTR(D.FECHA,1,4) AS ANIO,CASE WHEN CAST(SUBSTR(D.FECHA,6,2) AS INTEGER)<=6 THEN 'H1' ELSE 'H2' END AS SEMESTRE,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY ANIO,SEMESTRE ORDER BY ANIO DESC,SEMESTRE LIMIT 20","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_142","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año y semana","¿Qué artículos son más transaccionales por mes, año y semana?","Artículos con más líneas en DOCLIN por mes, año y semana.","SELECT STRFTIME('%Y-W%W',D.FECHA) AS SEMANA,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY SEMANA ORDER BY SEMANA DESC LIMIT 26","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_143","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año y día","¿Qué artículos son más transaccionales por mes, año y día?","Artículos con más líneas en DOCLIN por mes, año y día.","SELECT D.FECHA,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY D.FECHA ORDER BY D.FECHA DESC LIMIT 30","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_144","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año y hora","¿Qué artículos son más transaccionales por mes, año y hora?","Artículos con más líneas en DOCLIN por mes, año y hora (si hay timestamp).","SELECT SUBSTR(D.FECHA,1,7) AS MES,SUBSTR(D.FECHA,12,2) AS HORA,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE LENGTH(D.FECHA)>10 GROUP BY MES,HORA ORDER BY MES DESC,HORA LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_145","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año y día de la semana","¿Qué artículos son más transaccionales por mes, año y día de la semana?","Artículos con más líneas en DOCLIN por mes, año y día de la semana.","SELECT SUBSTR(D.FECHA,1,7) AS MES,CAST(STRFTIME('%w',D.FECHA) AS INTEGER) AS DIA_SEMANA,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY MES,DIA_SEMANA ORDER BY MES DESC,DIA_SEMANA LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_146","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año y día del mes","¿Qué artículos son más transaccionales por mes, año y día del mes?","Artículos con más líneas en DOCLIN por mes, año y día del mes.","SELECT SUBSTR(D.FECHA,1,7) AS MES,CAST(SUBSTR(D.FECHA,9,2) AS INTEGER) AS DIA_MES,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY MES,DIA_MES ORDER BY MES DESC,DIA_MES LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_147","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año y semana del mes","¿Qué artículos son más transaccionales por mes, año y semana del mes?","Artículos con más líneas en DOCLIN por mes, año y semana del mes.","SELECT SUBSTR(D.FECHA,1,7) AS MES,CAST((CAST(SUBSTR(D.FECHA,9,2) AS INTEGER)-1)/7 AS INTEGER)+1 AS SEMANA_MES,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY MES,SEMANA_MES ORDER BY MES DESC,SEMANA_MES LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_148","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año y trimestre del mes","¿Qué artículos son más transaccionales por mes, año y trimestre del mes?","Artículos con más líneas en DOCLIN por mes, año y trimestre del mes.","SELECT SUBSTR(D.FECHA,1,7) AS MES,CASE WHEN CAST(SUBSTR(D.FECHA,9,2) AS INTEGER)<=10 THEN 'Primera decena' WHEN CAST(SUBSTR(D.FECHA,9,2) AS INTEGER)<=20 THEN 'Segunda decena' ELSE 'Tercera decena' END AS DECENA,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY MES,DECENA ORDER BY MES DESC,DECENA LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_149","Artículos con mayor número de líneas en todos los documentos de todos los tipos por año y mes y año y semestre del mes","¿Qué artículos son más transaccionales por mes, año y semestre del mes?","Artículos con más líneas en DOCLIN por mes, año y semestre del mes.","SELECT SUBSTR(D.FECHA,1,7) AS MES,CASE WHEN CAST(SUBSTR(D.FECHA,9,2) AS INTEGER)<=15 THEN 'Primera quincena' ELSE 'Segunda quincena' END AS QUINCENA,COUNT(DISTINCT L.CODARTICULO) AS N_ARTICULOS,COUNT(*) AS N_LINEAS FROM DOCLIN L JOIN DOCCAB D ON D.CODIGO=L.CODDOCUMENTO WHERE D.FECHA IS NOT NULL GROUP BY MES,QUINCENA ORDER BY MES DESC,QUINCENA LIMIT 60","Compras","Dirección","Artículo","Bajo","Popularidad",""),
    q("cx3_150","Dashboard de compras completo","¿Cuál es el estado completo de compras?","KPIs de compras: proveedores, artículos, STOCKARTICULO, valor, reposición.","SELECT (SELECT COUNT(*) FROM PROVEED) AS N_PROVEEDORES,(SELECT COUNT(*) FROM ARTICULO) AS N_ARTICULOS,(SELECT COUNT(*) FROM ARTICULO WHERE STOCKARTICULO>0) AS CON_STOCKARTICULO,(SELECT ROUND(SUM(STOCKARTICULO*PRECIOCOSTE),2) FROM ARTICULO WHERE STOCKARTICULO>0 AND PRECIOCOSTE>0) AS VALOR_INVENTARIO,(SELECT COUNT(*) FROM ARTICULO WHERE STOCKARTICULO>0 AND STOCKARTICULO<STOCKARTICULO) AS BAJO_MINIMO,(SELECT COUNT(*) FROM ARTICULO WHERE STOCKARTICULO<0) AS STOCK_NEGATIVO","Compras","Dirección","KPI","Crítico","Dashboard",""),
]
