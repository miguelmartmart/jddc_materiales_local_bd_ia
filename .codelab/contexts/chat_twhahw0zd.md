```text
## Summary and Next Steps

The script loads the content of the `altacapacidad` folder and displays it in the browser.  The script uses relative paths to access the files and folders.  The script is designed to be a simple example and can be extended to include more functionality.  The script will execute the following steps:
1. Check if the `altacapacidad` folder exists.
2. If the `altacapacidad` folder exists, display the content of the folder.
```

## ## Resumen y Próximos Pasos

The script loads the content of the `altacapacidad` folder and displays it in the browser. The script uses relative paths to access the files and folders. The script is designed to be a simple example and can be extended to include more functionality. The script will execute the following steps:
1. Check if the `altacapacidad` folder exists.
2. If the `altacapacidad` folder exists, display the content of the folder.