fix(bat): ARRANCAR_DEVIA v3.3.0 - ultra-resiliente con Docker y npm

CORRECCIONES:
- Error "No se esperaba..." en opcion 2: goto dentro de bloques if()
  convertidos a etiquetas separadas (:ELECTRON_NO_CARPETA,
  :ELECTRON_SIN_MODULES, :ELECTRON_COMPILAR, :ELECTRON_LANZAR,
  :ELECTRON_SKIP_NPM)
- Todos los goto dentro de if() eliminados (causa del error original)
- Variables con ! usadas consistentemente (delayed expansion)

NUEVAS FUNCIONALIDADES:
- Docker no corriendo -> pregunta si quiere iniciarlo
  * Detecta si Docker esta instalado pero apagado
  * Ofrece iniciarlo (Enter = N por defecto, no critico)
  * Busca Docker Desktop.exe en rutas habituales
  * Espera hasta 60s con feedback de progreso
  * Si no arranca, continua sin Docker (no bloquea el arranque)

- node_modules ausente -> pregunta si quiere npm install
  * Detecta ausencia de node_modules en desktop-codelab
  * Ofrece ejecutar npm install automaticamente (Enter = S)
  * Guarda log en logs/npm_install.log
  * Si falla, continua sin Electron (chat web sigue disponible)

- Diagnostico ampliado: [4] Docker detallado, [8] Node.js/npm
- Verificacion de desktop-codelab/backend/server.py antes de lanzar
- :CHECK_AND_START_DOCKER nueva subrutina (no critica)
- :CHECK_DOCKER_DETAIL para diagnostico completo
- :CHECK_NODE para verificar Node.js y npm
